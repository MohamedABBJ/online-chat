import AWS from "aws-sdk";

const updateProfilePicture = async () => {
  const bucketName = "online-chat-bucket";
  const region = "us-east-1";

  AWS.config.update({
    //TODO:continue here
  });
};

export default updateProfilePicture;
